using System;
using System.Collections.Generic;
using ServiceStack.Model;

namespace GestionProyectos.Modelos.Interfaces
{
	/*
	public interface IGestor
	{
		TResponse Crear<TPeticion,TResponse>(TPeticion peticion);
		TResponse Actualizar<TPeticion, TResponse>(TPeticion peticion);
		TResponse Borrar<TPeticion, TResponse>(TPeticion peticion);
		TResponse Consultar<TPeticion, TResponse>(TPeticion peticion);
	}
	*/
}

